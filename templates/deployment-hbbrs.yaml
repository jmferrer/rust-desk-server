apiVersion: apps/v1
kind: Deployment
metadata:
  name: rust-hbbrs-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: rust-hbbrs-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rust-hbbrs-service
    spec:
      containers:
        - args:
            - hbbr
            - -k
            - _
          env:
            - name: PGID
              value: "100"
            - name: PUID
              value: "1024"
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          name: rust-hbbr-service
          ports:
            - containerPort: 21117
            - containerPort: 21119
          resources:
            {{- toYaml .Values.hbbr.ressources | nindent 12 }}
          volumeMounts:
            - mountPath: /root
              name: rustdesk-data
        - args:
            - hbbs
            - -r
            - rust-hbbr-service.svc:21117
            - -k
            - _
          env:
            - name: PGID
              value: "100"
            - name: PUID
              value: "1024"
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          name: rust-hbbs-service
          ports:
            - containerPort: 21115
            - containerPort: 21116
            - containerPort: 21116
              protocol: UDP
            - containerPort: 21118
          resources:
            {{- toYaml .Values.hbbs.ressources | nindent 12 }}
          volumeMounts:
            - mountPath: /root
              name: rustdesk-data
      restartPolicy: Always
      volumes:
        - name: rustdesk-data
          persistentVolumeClaim:
            claimName: rustdesk-data
      restartPolicy: Always
      volumes:
        - name: rustdesk-data
          persistentVolumeClaim:
            claimName: rustdesk-data
